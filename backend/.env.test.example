# Test environment placeholders only.
# Integration tests create DATABASE_URL dynamically with Testcontainers.
NODE_ENV=test
JWT_SECRET="replace-with-a-local-test-secret"

# Do not point integration tests at production or Supabase databases.
# DATABASE_URL is intentionally omitted for local integration tests.
